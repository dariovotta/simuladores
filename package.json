{
  "name": "misfinanzas-simuladores",
  "version": "1.0.0",
  "private": true,
  "description": "Simuladores financieros: comisiones de brokers, rotación de ONs, duration de bonos, LECAPs y carry trade.",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve -l 5173 public",
    "test": "node --test tests/*.test.js",
    "deploy": "npx --yes wrangler deploy"
  }
}
